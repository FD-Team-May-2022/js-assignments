function squareDiagonal(a) {
    return Math.sqrt(2)*a
}

console.log("The diagonal of a square where the length of each side is 9 is equal to " + squareDiagonal(9))

function triangleArea(a, b, c) {
    let base = a;
    // let h =
}

function circumference (r) {
    const PI = Math.PI;
    return 2 * PI * r
}

function areaCircle(r) {
    const PI = Math.PI
    return PI * r * r
}

console.log("Circumference of a circle whose radius is 4 : " + circumference(4) )

console.log("Circumference and surface area of a circle whose radius is 4 : " + areaCircle(4))